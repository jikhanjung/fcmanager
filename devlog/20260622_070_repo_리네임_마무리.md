# 20260622_070 — repo 리네임 마무리 (FcSky → fcmanager)

> GitHub repo `jikhanjung/FcSky` → `jikhanjung/fcmanager` 웹 리네임을 끝내고,
> 코드베이스에 남아 있던 리네임 누락분을 정리.

## 배경

리브랜딩(devlog 062)에서 이미지·서버경로·백업·도메인은 모두 `fcmanager` 로 전환했으나,
GitHub repo 디렉터리명(`FcSky`)·로컬 환경은 "나중에"로 미뤄둔 상태였다(HANDOFF "리네임 진행 중").
이번에 웹에서 repo 를 리네임하면서 남은 항목을 확인·정리.

## 확인 (m710q, 이미 완료된 것)

- `git remote` → `git@github.com:jikhanjung/fcmanager.git` (fetch/push 정상)
- 로컬 디렉터리 → `~/projects/fcmanager`
- venv → `~/venv/fcmanager` (구 `~/venv/FcSky` 제거됨)

## 변경

- **`deploy/Dockerfile`**: OCI 라벨 `org.opencontainers.image.source` 가 옛 URL
  `github.com/honestjung/fcsky`(owner·name 둘 다 옛것)를 가리켜서
  → `github.com/jikhanjung/fcmanager` 로 정정. repo 리네임으로 실제 깨져 있던 유일한 코드 참조.
- **`HANDOFF.md`**: "리네임 진행 중" 체크리스트를 완료 상태로 갱신, venv 설명 정정,
  dolfinid(운영) 소스 체크아웃(`~/projects/FcSky`)도 별도로 `git remote set-url` 필요하다는 메모 추가.

## 일부러 유지

- **클럽 슬러그 `fcsky`** (forms/tests/마이그레이션 `0002_backfill_fcsky` 등) — "FC Sky"는 한 클럽.
- **devlog/** — 과거 기록이라 불변.
- **레거시 `/FcSky/` 301 리다이렉트**(devlog 068) — 북마크 흡수용.
- **에셋 파일명** `static/img/fcskylogo.png`·`static/css/fcsky.css` — repo 리네임과 무관한 별개 건.
  나중에 클럽별 로고 + FCManager CI 작업 때 함께 정리.

## 잔여

- dolfinid 운영 호스트의 소스 체크아웃(`~/projects/FcSky`)에서 `git remote set-url` (GitHub 리네임
  시 redirect 는 동작하나 명시 권장). 단 앱은 이미지에 포함 → 배포엔 영향 없음.

## 커밋

`0f2dbc6` chore: repo 리네임(FcSky→fcmanager) 마무리 — Dockerfile OCI source·HANDOFF 체크리스트
