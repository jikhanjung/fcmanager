# Phase 1 — 프로젝트 셋업 작업 기록

- 작성일: 2026-06-14
- 문서 종류: 작업 기록 (Work log)
- 관련 계획: [20260614_P01_프로젝트_계획.md](20260614_P01_프로젝트_계획.md)

## 작업 요약

Phase 1(기반 + 데이터 입력)을 완료하고 개발 서버에서 동작을 확인했다.

## 1. 환경 구성

- 가상환경 `~/venv/FcSky` 사용 (사용자 지정). 초기에 만든 로컬 `.venv`는 삭제.
- 설치: `Django==5.1.15`, `Pillow` (이미지 업로드용). `requirements.txt` 생성.
  ```
  asgiref==3.11.1
  Django==5.1.15
  pillow==12.2.0
  sqlparse==0.5.5
  ```

## 2. 프로젝트 / 앱 생성

```
django-admin startproject config .
apps/teams         (Team, Player, TeamMembership)
apps/competitions  (Season, Competition, CompetitionEntry, Award)
apps/matches       (Opponent, Match, MatchEvent)
```

- 앱을 `apps/` 하위에 두어 `AppConfig.name = 'apps.<app>'`로 지정, `INSTALLED_APPS`에 등록.

## 3. settings 변경

- `LANGUAGE_CODE = 'ko-kr'`, `TIME_ZONE = 'Asia/Seoul'`
- `TEMPLATES['DIRS'] = [BASE_DIR / 'templates']`
- 정적/미디어 설정: `STATICFILES_DIRS`, `STATIC_ROOT`, `MEDIA_URL`, `MEDIA_ROOT`

## 4. 모델 구현

계획서의 도메인 모델을 그대로 구현. 주요 결정:

- 등번호·주장·합류일은 `TeamMembership`(선수↔팀↔시즌)에 둠 → 시즌·팀 이동 표현.
- `Match.result` 프로퍼티로 우리 팀 기준 승(W)/무(D)/패(L) 자동 판정.
- `MatchEvent`(side/event_type/minute/player)를 1단계부터 추가 → Phase 4 실시간 중계 기반.
- 한글 `verbose_name` 전면 적용.

## 5. 관리자(Admin) 구성

- 모든 모델 등록. `list_display` / `list_filter` / `search_fields` / `date_hierarchy`.
- 인라인: 팀·선수에 `TeamMembership`, 대회에 `CompetitionEntry`, 경기에 `MatchEvent`.
- `autocomplete_fields`로 FK 선택 편의 제공.
- Admin 헤더를 "FC Sky 관리자"로 커스터마이징.

## 6. URL / 뷰 / 템플릿

- `config/urls.py`: admin + `apps.teams.urls` include, DEBUG 시 미디어 서빙.
- `apps/teams/urls.py`: `home`, `team_list`, `team_detail`.
- 뷰: 홈(다가오는 경기 5 / 최근 결과 5 / 팀 목록), 팀 목록, 팀 상세(선수단 표).
- 템플릿: `base.html`(Bootstrap 5, 내비), `home.html`, `team_list.html`, `team_detail.html`.

## 7. 마이그레이션 / 관리자 계정

- `makemigrations` + `migrate` 완료.
- 개발용 superuser 생성: `admin` / `fcsky1234` (개발 전용, 운영 시 교체 필요).

## 8. 검증

- `manage.py check` → 이슈 0.
- 개발 서버 스모크 테스트: `/` 200, `/teams/` 200, `/admin/` 302(로그인 리다이렉트, 정상).

## 부수 작업

- `.gitignore`, `README.md` 작성.

## 다음 단계

- 샘플 데이터 시드 스크립트(선택) → Phase 2(공개 조회 페이지: 선수단/일정·결과/순위/명예의 전당) 착수.
