# 원격 원터치 배포 도입 + 0.6.12/0.6.13 운영 배포 — git-free 파이프라인 실전 검증

날짜: 2026-07-13
연관: devlog 082(Track A 구현)·083(Track B)·084(점검 페이지 배선). 이 로그는 그 결과물의
**실제 운영 배포 실행 기록** — 082 의 "다음 배포 절차"가 계획이었다면 여기는 실행과 결과.

## remote-prod.sh (커밋 b4c6304)

cdGTS `deploy/remote-prod.sh` 와 동일 구성의 얇은 래퍼 도입 —
`./deploy/remote-prod.sh X.Y.Z` = `ssh dolfinid '/srv/fcmanager/deploy-prod.sh X.Y.Z'`.
`PROD_HOST`/`PROD_DEPLOY` env 로 대상 변경. fcmanager 는 `has_seed=false` 라 cdGTS 의
`--reseed` 만 제외. deploy.toml·DEPLOY.md·README·build.sh 안내를 이 기준으로 배선.
m710q→dolfinid SSH 키 인증(BatchMode) 사전 확인.

## 0.6.12 — 첫 git-free 배포 (부트스트랩 + 게이트 false-fail 발견)

- 부트스트랩: dolfinid 에 `deploy-prod.sh`·`_extract_and_deploy.sh` 심음(최초 1회).
- `./deploy/remote-prod.sh 0.6.12` 실행 — [0/7] 이미지 추출·self-heal, 스냅샷(448K),
  스왑까지 정상. 그러나 **[6/7] DB 바인딩 게이트가 false-fail 로 중단**:
  - 원인: 게이트가 순수 `python -c` 로 settings 를 읽는데, fcmanager 컨테이너에는
    `DJANGO_SETTINGS_MODULE` env 가 없어(fsis 는 compose 에 명시) ImproperlyConfigured
    → 빈 값 → FATAL exit. **배포 자체는 정상**이었음 — 수동 smoke PASS
    (`version=0.6.12, db=ok, club=1, match=28`), 공개 사이트 v0.6.12 확인.
  - 수정(커밋 66d1bb6): `manage.py shell -c` 경유로 교체. 운영 컨테이너에서
    `/app/db.sqlite3` 정상 반환 확인. 재배포 없이 다음 릴리스 self-heal 로 반영.
- 부수 발견: 운영 `.env` 는 `HOST_PORT=8004`(8003 은 과거 병렬 운영 잔재) —
  스크립트들은 `.env` 를 읽어 실동작 무관, deploy.toml 표기만 교정.
  교훈: **매니페스트도 기억으로 쓰면 안 된다** — 작성 시 운영 현물(.env) 대조.

## 0.6.13 — 전 구간 완주 검증 (버전만 bump)

preflight(go — host 스크립트 변경만, seed lint 🟢) → `build.sh 0.6.13`(테스트 전체 통과
→ bump f5006ad → push `sha256:a4baf5e3…`) → `remote-prod.sh 0.6.13`:
[0/7] 추출·self-heal(게이트 수정 반영) → 스냅샷 → 스왑 → [6/7] **게이트 OK**
(`/app/db.sqlite3`) → **[7/7] smoke PASS**(`version=0.6.13, db=ok, club=1, match=28`).
공개 사이트 v0.6.13 확인. **fcmanager 상시 배포 = `build.sh X.Y.Z` + `remote-prod.sh X.Y.Z`
두 줄로 확정.** dolfinid repo 체크아웃은 배포 경로에서 완전 이탈(삭제 가능).

## 계약 문서 피드백 (devdocs 978f855)

fcmanager 정렬 완료를 `deploy-data-contract.md` 에 반영 — 활성 롤아웃 3/3:
- 첫 `has_seed=false` 케이스(불변식 = seed 명령 부재, §동사 seed).
- seed 은퇴 조건 정정(배치 UI 전제 아님 — in-app 입구 존재 + 운영 반영 확인).
- 이식 함정 2건 문서화: DB 게이트 `python -c` false-fail(§동사 deploy),
  멀티테넌트 `/healthz` 예약 세그먼트(§적용 절차 7).
