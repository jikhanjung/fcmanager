# 20260617_062 — 리브랜딩: FcSky → FCManager (SaaS)

> 멀티테넌트 SaaS로서의 제품명을 **FCManager**로 확정. "FC Sky"는 이제 **한 클럽**(slug `fcsky`)일 뿐.
> repo 디렉터리명(`FcSky`)은 나중에 변경.

## 바꾼 것

- **docker 이미지**: `honestjung/fcsky` → **`honestjung/fcmanager`** (compose 2종·Dockerfile LABEL·deploy.sh·docs).
  버전 0.6.0.
- **플랫폼/SaaS 이름 = FCManager**: base.html 브랜드·`<title>`·footer 폴백, 랜딩 h1,
  admin 헤더, child 타이틀 기본값. **클럽 페이지는 `current_club` 기반**(FC Sky 클럽은 그대로 "FC Sky").
- **서버 경로**: `/srv/FcSky` → **`/srv/fcmanager`** (compose volumes·deploy.sh·sync_to_srv·backup_db.py·nginx alias).
  container_name `fcsky`→`fcmanager`.
- **백업 경로/스크립트**: `~/backups/FcSky`→`~/backups/fcmanager`, NAS `fcmanager_backup`,
  `dev_data/fcmanager`, 스냅 프리픽스 `fcsky_`→`fcmanager_`, env override `FCMANAGER_REMOTE_*`.
  스크립트 파일명 `backup-fcsky.sh` → **`backup-fcmanager.sh`**, backup_db 트랙 `fcsky`→`fcmanager`.
- 문서(README·CLAUDE·deploy.md·backup.md) 앱 지칭 "FC Sky" → "FCManager"(클럽명 문자열은 보존).

## 보존(클럽 데이터)
- `Club(slug="fcsky", name="FC Sky")` — 마이그레이션 0002·tests의 클럽 이름은 그대로.
- repo 체크아웃 경로 `~/projects/FcSky`(리네임 예정), github source URL.

## 검증
- `check` 무이슈, 스크립트 문법 OK, **전체 28개 테스트 통과**.
- 템플릿 하드코딩 'FC Sky' 0(전부 current_club 또는 FCManager).

## 운영 전환 시 함께 할 일 (dolfinid/m710q, 미완료)
- dolfinid: `mv /srv/FcSky /srv/fcmanager`, hourly cron 경로를 `/srv/fcmanager/...`로.
- m710q: `mv ~/backups/FcSky ~/backups/fcmanager`, `cp scripts/backup-fcmanager.sh ~/scripts/`,
  cron `backup-fcsky.sh`→`backup-fcmanager.sh` 교체.
- 도메인 전환(fcmanager.app) + 이미지 0.6.0 배포는 `docs/operation_manual/deploy.md` 참고.
