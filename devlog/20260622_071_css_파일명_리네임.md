# 20260622_071 — CSS 파일명 리네임 (fcsky.css → fcmanager.css)

repo 리네임(FcSky→fcmanager, devlog 070) 마무리 후 현행 코드에 남아 있던
구 이름 정리.

## 변경

- `static/css/fcsky.css` → `static/css/fcmanager.css` (`git mv`)
- `templates/base.html` 의 `{% static 'css/fcsky.css' %}` 참조 갱신
- 내부 keyframe `fcsky-rise` → `fcmanager-rise` (정의 + `.reveal` 사용처)

`python manage.py check` 통과.

## 의도적으로 유지 (리네임 대상 아님)

- `devlog/**` — 과거 기록이라 불변
- 레거시 `/FcSky/` 301 리다이렉트, 클럽 슬러그 `fcsky` — 운영상 유지
- `static/img/fcskylogo.png` — FC Sky 클럽 실제 엠블럼(플랫폼명 아님)
- 마이그레이션 `0002_backfill_fcsky` 등 — 불변
- `apps/clubs/forms.py` 슬러그 placeholder 예시(`fcsky`), 테스트의 `/fcsky/` 경로 — 클럽 슬러그
