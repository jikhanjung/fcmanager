# 20260615_017 — 선수 사진 일괄 반영 (import_player_photos)

## 추가
- **`apps/teams/management/commands/import_player_photos.py`** — 저장된 KFA 참가신청
  페이지(`matchApplyTeam.html`)를 파싱해 선수 사진을 `Player.photo`에 일괄 반영(멱등).
  - 카드 구조(`<img src="...사진_xxx.jpg"> + <h4>이름　No.번호</h4>`)를 정규식으로 추출.
  - 이름으로 매칭, KFA 동명이인 접미숫자 정규화(예: `이상호1`→`이상호`).
  - 기본 아이콘(사진 없음)은 건너뜀. 옵션: `--team` `--overwrite` `--dry-run`.

## 결과 (K7)
- 34명 자동 반영 + 직접 제공분(강민혁) 1명 = **K7 35/35 전원 사진**.
- 사진 원본(`data/sky.zip`)은 gitignore라 컨테이너로 복사해 실행.

## 미디어 서빙 (운영)
- 기존 prod 미서빙 → 미디어를 호스트 영속 경로 **`/srv/FcSky/media`**(uid 1000)로 bind,
  **nginx `/FcSky/media/` alias** 직접 서빙(자세히는 devlog 019).
- `player_detail.html`은 기존부터 `player.photo` 표시.

## 테스트
- `--dry-run` 34건 매칭(실패 0) → 실제 반영 → `/FcSky/media/players/*` 200(image),
  선수 상세 페이지 사진 렌더 확인.
