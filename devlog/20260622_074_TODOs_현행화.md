# 20260622_074 — TODOs.md 현행화

> devlog 073(README·CLAUDE.md 현행화)에 이어 `TODOs.md` 도 실제 코드/운영 상태에 맞춤.

## 변경

- **시즌 → 연도 표기 정정** (Season 모델 제거 반영). 실제 라우팅이 `/seasons/` →
  `/years/`(`year_index`/`year_detail`)로 바뀐 것 확인 후:
  - 일정·결과 필터, 득점 순위 필터: `시즌별` → `연도별`
  - 명예의 전당: `시즌·대회별` → `연도·대회별`
  - 아카이브: `시즌별(/seasons/)` → `연도별(/years/ + /years/<year>/)`
- **이번 정리 작업 완료 항목 추가**:
  - m710q 구 cron `backup-fcsky.sh`(04시) 제거·retired (`backup-fcmanager.sh` 05시로 단일화)
  - repo 리네임 FcSky → `fcmanager`(m710q·dolfinid·GitHub·Dockerfile) + 문서 현행화 완료
    (devlog 070·072·073)

## 그대로 둔 것

- `honestjung/fcsky:0.1.0`, "fcsky 컨테이너 기동/레거시 유지" 등은 **당시 수행 작업의 역사적
  기록**이라 보존(이후 변경은 별도 `[x]` 항목으로 이미 반영).

## 결과

README·CLAUDE.md·HANDOFF·TODOs 네 문서가 모두 현재 코드/운영 상태와 일치.

## 커밋

`61e06d0` docs: TODOs 현행화 — 시즌→연도 표기 정정 + 리네임/문서정리 완료 반영
