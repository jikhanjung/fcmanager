# 20260615_010 — 테스트 코드 (모델·뷰)

## 추가
- `matches.tests.MatchResultTest` — Match.result 승/무/패/미정.
- `competitions.tests.StandingsTest` — 조 순위 집계(우리 경기 + 상대팀 간 경기 합산),
  정렬/승점/포함 여부.
- `teams.tests.PublicPagesSmokeTest` — seed 후 공개 페이지 7종 + 상세 3종 200,
  seed 멱등성.

## 설정 보정
- WhiteNoise `CompressedManifestStaticFilesStorage`는 테스트 시 collectstatic 산출물이
  없어 `{% static %}` manifest 조회가 실패 → `settings`에서 **테스트('test' in sys.argv)일 때는
  기본 StaticFilesStorage** 사용하도록 분기. 개발/운영은 manifest 유지.

## 결과
- `python manage.py test` → 9 tests OK. (WhiteNoise의 staticfiles 디렉토리 부재 경고는 무해)
