# 20260615_016 — 선수 명단 일괄 입력 (import_roster) + Player.squad

## 추가
- **`Player.squad`** 필드(`CharField`, blank) + 마이그레이션 `teams.0002_player_squad`.
  - 대회 구분용 임시 필드(예: 50대초/50대말). **추후 대회별·팀별 로스터 모델로 분리 예정.**
  - admin PlayerAdmin에 `squad` 컬럼·필터 추가.
- **`apps/teams/management/commands/import_roster.py`** — CSV 로스터 일괄 입력(멱등).
  - 컬럼: `name, jersey, squad, position, note`. 이름으로 선수 매칭(get_or_create).
  - 등번호는 관례대로 **TeamMembership**에, squad/position/비고는 Player에 반영.
  - 옵션: `--team`(slug) `--season` `--create-team` `--dry-run`. 포지션 한글 별칭(키퍼→GK).

## 입력 결과 (스카이 50대 / 2026)
- 50대초 8 + 50대말 8 = **16명** (기존 4명 등번호·구분 갱신, 12명 신규).
- 키퍼→GK, 부상/"21일만 출전"→선수 비고에 반영.
- 원본 CSV는 `data/`(gitignore)라 컨테이너로 복사해 실행, 결과는 영속 DB에 저장.

## 테스트
- `--dry-run` 파싱 확인 → 실제 입력 → DB 16건 검증(구분/등번호/포지션/비고).
