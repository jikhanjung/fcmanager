# 20260617_058 — SaaS Phase B3~B5 (쓰기 주입·격리테스트·non-null 확정)

> 선행: [057 B2](20260617_057_SaaS_PhaseB2_쿼리스코핑.md). 이로써 Phase B(라우팅+스코핑) 완료.

## B3 — 쓰기 주입 + 폼 선택지 클럽 제한
- 생성 시 `club=request.club` 주입은 B2 에서 notice/team/player 반영 완료.
- **폼 누수 수정**: `MembershipAddForm` 의 선수 선택지를 팀의 클럽 선수로 제한
  (`Player.filter(club_id=team.club_id)`) — 타 클럽 선수 노출 차단.
- 그 외 club-소유 모델 생성은 admin 전용(Match·Gallery·Award·Entry) → B5 에서 club 필수화로 처리.

## B4 — 격리 테스트 확대
- `apps/clubs/tests.py` `TenantIsolationTest` 확대: notices 외 **팀 목록/상세·선수 상세**
  교차 클럽 404·목록 격리 검증. (총 6 테스트)

## B5 — club FK non-null 확정
- 6개 클럽 소유 모델(`Team`·`Player`·`Match`·`Notice`·`GalleryItem`·`Award`)의 `club` FK 를
  `null=False` 로 확정. 마이그레이션:
  `teams 0007`·`matches 0014`·`notices 0003`·`gallery 0003`·`competitions 0008`.
- 사전 점검: 전 모델 `club__isnull` 0건 → 안전.

## 검증
- `migrate` OK, `check` 무이슈, `makemigrations --check` 변경 없음(모델↔마이그레이션 일치).
- **전체 24개 테스트 통과**.

## Phase 6 진행
- A(테넌트 모델)·**B(라우팅+스코핑) 완료**.
- 남음: **C**(클럽별 권한 `ClubMembership` 적용 + 클럽 생성/온보딩), **D**(브랜딩 분리).
- 운영 전환(배포) 메모: `DJANGO_URL_PREFIX` 제거(슬러그가 대체), nginx `/FcSky/`→`/fcsky/`
  리다이렉트 + static/media alias 조정 — 배포 시 일괄.
